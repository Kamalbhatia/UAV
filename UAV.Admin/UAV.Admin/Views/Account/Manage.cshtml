@model UAV.Admin.Models.LocalPasswordModel
@{
    ViewBag.Title = "Manage Account";
}


<div class="container">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="registrationSection">


            @*<div class="row bs-wizard custmWizrd">

                    <div class="col-xs-4 bs-wizard-step complete">

                        <div class="progress"><div class="progress-bar"></div></div>
                        <a href="@Url.Action("RegisterPersonalDetail", "JobSeeker")" class="bs-wizard-dot dotOne">1</a>

                    </div>

                    <div class="col-xs-4 bs-wizard-step complete">
                        <!-- complete -->

                        <div class="progress"><div class="progress-bar"></div></div>
                        <a href="@Url.Action("ContactPersonAddress", "JobSeeker")" class="bs-wizard-dot dotTwo">2</a>

                    </div>

                    <div class="col-xs-4 bs-wizard-step active">
                        <!-- complete -->

                        <div class="progress"><div class="progress-bar"></div></div>
                        <a href="@Url.Action("CreateYourAccount", "JobSeeker")" class="bs-wizard-dot dotThree">3</a>

                    </div>

                </div>*@

            <div class="formOuter">
                @*<div class="bckBtn"><a href=""><i class="fa fa-angle-left" aria-hidden="true"></i></a></div>*@
                <p class="message-success">@ViewBag.StatusMessage</p>

                <p>You're logged in as <strong>@User.Identity.Name</strong>.</p>
                @if (ViewBag.HasLocalPassword)
                {
                    @Html.Partial("_ChangePasswordPartial")
                }
                else
                {
                    @Html.Partial("_SetPasswordPartial")
                }


               
            </div>

        </div>
    </div>
</div>



@*<section id="externalLogins">
    @Html.Action("RemoveExternalLogins")

    <h3>Add an external login</h3>
    @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
</section>*@

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
